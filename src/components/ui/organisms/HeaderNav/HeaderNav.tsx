import type { HeaderNavProps } from './types';

export const HeaderNav = ({}: HeaderNavProps) => {
  return (
    <nav className='nav'>
      <p className='logo'>j-lytn / frontend developer</p>
      <p>[ Scrollable Motion Slider ]</p>
    </nav>
  );
};
