import type { LabProps } from './types';

export const Lab = ({}: LabProps) => {
  return (
    <div className='w-full h-screen flex items-center justify-center bg-cyan-500'>
      <h1 className='text-4xl font-bold text-white'>Lab</h1>
    </div>
  );
};
